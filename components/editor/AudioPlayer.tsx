// components/editor/AudioPlayer.tsx
export function AudioPlayer() {
  return (
    <div
      style={{
        height: '100%',
        display: 'flex',
        alignItems: 'center',
        padding: '0 8px',
        gap: 8,
        color: 'var(--muted)',
        fontSize: 11,
      }}
    >
      AudioPlayer — Task 9
    </div>
  );
}
